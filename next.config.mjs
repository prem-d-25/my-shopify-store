/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["cdn.shopify.com"], // Add other domains if needed
    // Optional: Specify other image configurations
    // formats: ['image/avif', 'image/webp'], // For example
  },
};

export default nextConfig;
